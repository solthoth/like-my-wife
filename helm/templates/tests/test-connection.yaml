apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "like-my-wife.fullname" . }}-test-connection"
  labels:
    {{- include "like-my-wife.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "like-my-wife.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
